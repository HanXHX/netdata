# Automatically added by dh_installinit
if [ -x "/etc/init.d/netdata" ] || [ -e "/etc/init/netdata.conf" ]; then
	if [ ! -e "/etc/init/netdata.conf" ]; then
		update-rc.d netdata defaults >/dev/null
	fi
	invoke-rc.d netdata start || exit $?
fi
# End automatically added section
